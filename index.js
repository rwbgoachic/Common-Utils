// @paysurity/common-utils
// Placeholder entry point

module.exports = {
  logInfo: (msg) => {
    console.log(`[INFO] ${msg}`);
  },
  logError: (msg) => {
    console.error(`[ERROR] ${msg}`);
  }
};
